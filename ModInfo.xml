<?xml version="1.0" encoding="UTF-8" ?>
<xml>
  <Name value="MaxChunkAgeDeadlockFix" />
  <DisplayName value="MaxChunkAge Deadlock Fix" />
  <Description value="Fixes the full server freeze caused by a lock-order deadlock between MultiBlockManager, RegionFileManager and the chunk unload path when MaxChunkAge chunk reset is enabled. Contended locks are taken non-blocking and the work is replayed on the main thread instead of being dropped. Adds chunk reset logging and a main thread watchdog. See README.md for details." />
  <Author value="kotfoxtrot" />
  <Version value="1.4.0" />
  <Website value="" />
</xml>
